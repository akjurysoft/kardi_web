/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:['localhost', 'kardify1.b2cinfohosting.in', 'kardify.netlify.app', '54.159.106.52'],
    },
};

export default nextConfig;
